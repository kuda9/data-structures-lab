section:1904
name:Boyd Bwalya Kaoma
roll number: 14
#include<stdio.h>
int main()
{
int array1[10],array2[10],array3[10],m,n,i,j,position;
printf("Enter size of array1\n");
scanf("%d",&n);
printf("Enter size of array2\n");
scanf("%d",&m);
printf("Enter %d elements into array1\n",n);
for(i=1;i<=n;i++)
scanf("%d",&array1[n]);
printf("Enter %d elements into array2\n",m);
for(j=1;j<=m;j++)
scanf("%d",&array2[j]);

  printf("Sorted array:\n");
  for (i = 0; i < m + n; i++) {
    printf("%d\n", array3[i]);

   printf("Enter the location where you wish to delete element\n");
   scanf("%d", &position);
   if (position >= n+1)
      printf("Deletion not possible.\n");
   else
   {
      for (i = position - 1; i < n - 1; i++)
         array1[i] = array1[i+1];

      printf("Resultant array:\n");

      for (i = 0; i < n - 1; i++)
         printf("%d\n", array1[i]);
   }

   return 0;
}
}
